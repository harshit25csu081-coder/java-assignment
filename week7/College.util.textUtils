if (ch == ' ') {
                if (!space) {
                    str.append(ch);
                    space = true;
                }
            } else {
                str.append(ch);
                space = false;
            }
        }